import { DEFAULT_AUTHENTICATION_STATE } from "../config/defaultStates";


export default function authentication(
  state = DEFAULT_AUTHENTICATION_STATE,
  action
) {
  switch (action.type) {
    case "ADD_NEW_DEVICE":
      return
  }
};
